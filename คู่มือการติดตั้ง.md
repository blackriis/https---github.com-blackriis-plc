# 📘 คู่มือการติดตั้งระบบวิเคราะห์ข้อมูลนักเรียน RT / NT / O-NET
### สำหรับผู้ที่ไม่เชี่ยวชาญคอมพิวเตอร์ (ทำตามทีละขั้น รับรองได้)

> ระบบนี้เป็น "เว็บแอป" ที่ทำงานบน Google ฟรี ไม่ต้องลงโปรแกรมอะไรในเครื่อง
> ใช้แค่ **บัญชี Gmail / Google** และ **เว็บเบราว์เซอร์ (แนะนำ Google Chrome)** เท่านั้น
>
> ⏱️ ใช้เวลาติดตั้งครั้งแรกประมาณ **15–20 นาที**

---

## 🧭 ภาพรวมก่อนเริ่ม (อ่านสัก 1 นาที)

ระบบประกอบด้วย **3 ส่วน** ที่ต้องอยู่ด้วยกัน:

| ส่วน | คืออะไร | เปรียบเหมือน |
|------|---------|--------------|
| 1. ไฟล์ **Google Sheet** | ที่เก็บคะแนนนักเรียน RT/NT/O-NET | "คลังข้อมูล" |
| 2. **Code.gs** | โปรแกรมหลังบ้าน (สมองของระบบ) | "เครื่องยนต์" |
| 3. **Index.html** | หน้าเว็บที่คนดู สวย ๆ มีกราฟ | "หน้าปัด/หน้าจอ" |

**หัวใจสำคัญที่ต้องเข้าใจ:** โปรแกรม (ข้อ 2–3) ต้อง **ฝังอยู่ในไฟล์ Google Sheet** (ข้อ 1)
เพราะระบบจะอ่านคะแนนจาก "ชีตที่ตัวเองอยู่" โดยตรง
➡️ **ดังนั้นต้องสร้างโปรแกรมจากในเมนูของ Google Sheet เท่านั้น** (เดี๋ยวสอนในขั้นตอนที่ 3)

---

## ✅ สิ่งที่ต้องเตรียมก่อน

1. บัญชี Google (Gmail) — เข้าใช้งานได้ปกติ
2. ไฟล์ 2 ไฟล์นี้ (อยู่ในโฟลเดอร์ที่ได้รับมา):
   - `Code.gs`
   - `Index.html`
3. ไฟล์คะแนนนักเรียน เช่น ผล RT, NT, O-NET (เป็น Excel หรือ Google Sheet ก็ได้)
4. เบราว์เซอร์ **Google Chrome** (แนะนำ เพราะมีปัญหาน้อยสุด)

> 💡 **วิธีเปิดอ่านเนื้อหาในไฟล์ Code.gs / Index.html:** คลิกขวาที่ไฟล์ → เปิดด้วย → โปรแกรม
> "TextEdit" (Mac) หรือ "Notepad" (Windows) จะเห็นเป็นตัวหนังสือยาว ๆ — **ไม่ต้องเข้าใจ** แค่ก๊อปปี้ทั้งหมด

---

## 🟦 ขั้นตอนที่ 1 — เตรียมไฟล์คะแนนให้เป็น Google Sheets

⚠️ **สำคัญมาก:** ระบบอ่านได้เฉพาะไฟล์ที่เป็น **Google Sheets** เท่านั้น
ถ้าเป็นไฟล์ Excel (`.xlsx`) ต้องแปลงก่อน ไม่งั้นระบบจะขึ้นว่า "ไม่พบชีตข้อมูลนักเรียน"

**วิธีทำ:**
1. เปิด **Google Drive** → พิมพ์ `drive.google.com` ในเบราว์เซอร์
2. ลากไฟล์คะแนน (Excel) เข้าไปวางในหน้า Drive เพื่ออัปโหลด
3. **ดับเบิลคลิก** เปิดไฟล์ที่เพิ่งอัปโหลด
4. ที่แถบเมนูบนสุด กด **ไฟล์ (File)** → **บันทึกเป็น Google ชีต (Save as Google Sheets)**
5. จะได้ไฟล์ใหม่ที่มีไอคอนสีเขียว 🟩 — **อันนี้แหละที่จะใช้** (ไฟล์ Excel เดิมทิ้งได้)

> 📌 ในไฟล์ Google Sheet นี้ ต้องมี "แท็บชีต" (แถบล่างจอ) ที่เก็บคะแนน เช่น
> - ชีต RT / NT — มีหัวตารางคำว่า **"ชื่อ-สกุล"** หรือ **"เลขที่นั่งสอบ"** หรือ **"ลำดับ"**
> - ชีต O-NET เช่น `IndividualScore_2568_P6` (ป.6) หรือ `IndividualScore_2568_M3` (ม.3)
>
> ระบบจะ **หาชีตที่เกี่ยวข้องให้อัตโนมัติ** ไม่ต้องตั้งชื่อชีตเป๊ะ ๆ ขอแค่มีหัวตารางตามด้านบน

---

## 🟦 ขั้นตอนที่ 2 — เปิดตัวแก้ไขโปรแกรม (Apps Script)

1. เปิดไฟล์ **Google Sheet** (ตัวสีเขียว จากขั้นตอนที่ 1) ให้ค้างไว้
2. ที่แถบเมนูบน กด **ส่วนขยาย (Extensions)**
3. เลือก **Apps Script**
4. จะมีแท็บใหม่เด้งขึ้นมา — นี่คือ "ห้องเครื่อง" ของระบบ

> 🖼️ หน้าตาจะมีแถบซ้ายมือชื่อ "ไฟล์ (Files)" และมีไฟล์ชื่อ `Code.gs` อยู่แล้ว 1 ไฟล์

---

## 🟦 ขั้นตอนที่ 3 — วางโค้ด Code.gs

1. ในแถบซ้าย คลิกไฟล์ **`Code.gs`** (หรือ `รหัส.gs`)
2. คลิกในช่องเขียนโค้ดฝั่งขวา แล้ว **ลบทุกอย่างที่มีอยู่ทิ้ง**
   - กดในช่อง → กด `Ctrl + A` (Windows) หรือ `⌘ + A` (Mac) เพื่อเลือกทั้งหมด → กด `Delete`
3. เปิดไฟล์ **`Code.gs`** ที่ได้รับมา (ด้วย Notepad/TextEdit) → เลือกทั้งหมด (`Ctrl+A`) → ก๊อปปี้ (`Ctrl+C`)
4. กลับมาที่ช่องเขียนโค้ด → วาง (`Ctrl+V`)
5. กด **บันทึก** (ไอคอนแผ่นดิสก์ 💾 หรือ `Ctrl+S`)

---

## 🟦 ขั้นตอนที่ 4 — สร้างไฟล์หน้าเว็บ Index.html

⚠️ **ชื่อไฟล์ต้องเป็น `Index` เป๊ะ ๆ** (I ตัวใหญ่) ไม่งั้นระบบจะเปิดหน้าเว็บไม่ได้

1. ในแถบซ้าย มองหาหัวข้อ **"ไฟล์ (Files)"** → กดเครื่องหมาย **➕ (บวก)** ข้าง ๆ
2. เลือก **HTML**
3. จะให้ตั้งชื่อไฟล์ → พิมพ์ว่า **`Index`** (ไม่ต้องพิมพ์ `.html` ต่อท้าย ระบบเติมให้เอง) → กด Enter
4. ไฟล์ `Index.html` จะเปิดขึ้นมาพร้อมโค้ดตัวอย่าง — **ลบทิ้งทั้งหมด** (`Ctrl+A` → `Delete`)
5. เปิดไฟล์ **`Index.html`** ที่ได้รับมา → เลือกทั้งหมด → ก๊อปปี้ → วางลงในช่อง
6. กด **บันทึก** (`Ctrl+S`)

> ✅ ตอนนี้ในแถบซ้ายควรมี **2 ไฟล์**: `Code.gs` และ `Index.html`

---

## 🟦 ขั้นตอนที่ 5 — เผยแพร่เป็นเว็บแอป (Deploy)

ขั้นนี้คือการ "เปิดร้าน" ให้ระบบมีลิงก์เว็บสำหรับเข้าใช้งานจริง

1. มุมขวาบน กดปุ่มสีน้ำเงิน **ทำให้ใช้งานได้ (Deploy)** → เลือก **การทำให้ใช้งานได้ใหม่ (New deployment)**
2. กดไอคอน **⚙️ เฟือง** (ข้างคำว่า "เลือกประเภท / Select type") → เลือก **เว็บแอป (Web app)**
3. ตั้งค่าดังนี้:
   - **คำอธิบาย (Description):** พิมพ์อะไรก็ได้ เช่น `ระบบวิเคราะห์นักเรียน`
   - **ดำเนินการในฐานะ (Execute as):** เลือก **ฉัน (Me / อีเมลคุณ)**
   - **ผู้ที่มีสิทธิ์เข้าถึง (Who has access):**
     - เลือก **ทุกคน (Anyone)** ถ้าอยากให้ครูคนอื่นเปิดลิงก์ได้โดยไม่ต้องล็อกอิน
     - หรือเลือก **ทุกคนในองค์กร** / **เฉพาะฉัน** ถ้าต้องการจำกัดสิทธิ์
4. กด **ทำให้ใช้งานได้ (Deploy)**

---

## 🟦 ขั้นตอนที่ 6 — อนุญาตสิทธิ์ (ครั้งแรกครั้งเดียว)

Google จะถามขออนุญาตให้โปรแกรมอ่านไฟล์ชีตของคุณ — เป็นเรื่องปกติ ให้ทำตามนี้:

1. กด **ให้สิทธิ์การเข้าถึง (Authorize access)**
2. เลือกบัญชี Google ของคุณ
3. ถ้าขึ้นหน้าจอ **"Google ยังไม่ได้ยืนยันแอปนี้ (Google hasn't verified this app)"**
   - กด **ขั้นสูง (Advanced)** ที่มุมล่างซ้าย
   - กด **ไปที่ ... (ไม่ปลอดภัย) / Go to ... (unsafe)**
   - 👉 ไม่ต้องกังวล เพราะเป็นแอปที่ **คุณสร้างเอง** ไม่ใช่ของคนแปลกหน้า
4. กด **อนุญาต (Allow)**

---

## 🟦 ขั้นตอนที่ 7 — คัดลอกลิงก์และเปิดใช้งาน

1. เมื่อ Deploy สำเร็จ จะขึ้นกล่อง **URL ของเว็บแอป (Web app URL)** ที่ลงท้ายด้วย `.../exec`
2. กด **คัดลอก (Copy)**
3. เปิดแท็บใหม่ในเบราว์เซอร์ → วางลิงก์ → กด Enter
4. 🎉 **เสร็จแล้ว!** ระบบจะแสดงหน้าวิเคราะห์ข้อมูลนักเรียนพร้อมกราฟ

> 💾 **เก็บลิงก์นี้ไว้ให้ดี** (บันทึกใน Google Keep / จดไว้ / ส่งเข้าไลน์ตัวเอง)
> นี่คือลิงก์ที่จะส่งให้ครูคนอื่นเปิดใช้งานได้เลย

---

## 🤖 (ทางเลือก) เปิดฟีเจอร์ AI ช่วยแนะนำ

ระบบสามารถใช้ AI ช่วยเขียนคำแนะนำการพัฒนานักเรียนได้ **แต่ต้องมีคีย์ของ OpenAI (มีค่าใช้จ่าย)**
ถ้า **ไม่ใส่ ระบบก็ทำงานได้ปกติ** เพียงแต่ไม่มีข้อความแนะนำจาก AI

**วิธีเพิ่มคีย์ (ถ้ามี):**
1. กลับไปที่หน้า Apps Script
2. แถบซ้าย กด **⚙️ การตั้งค่าโปรเจ็กต์ (Project Settings)**
3. เลื่อนลงหาหัวข้อ **คุณสมบัติของสคริปต์ (Script Properties)** → กด **เพิ่มคุณสมบัติสคริปต์**
4. ช่อง **ชื่อ (Property):** พิมพ์ `OPENAI_API_KEY` (ต้องสะกดแบบนี้เป๊ะ ๆ ตัวใหญ่ทั้งหมด)
5. ช่อง **ค่า (Value):** วางคีย์ที่ได้จาก OpenAI (ขึ้นต้นด้วย `sk-...`)
6. กด **บันทึกคุณสมบัติสคริปต์ (Save script properties)**

---

## 🔄 วิธีอัปเดตเวอร์ชันใหม่ (เมื่อมีไฟล์ Code.gs / Index.html รุ่นใหม่)

**ไม่ต้องทำใหม่ทั้งหมด** แค่เปลี่ยนโค้ดแล้วปล่อยเวอร์ชันใหม่:

1. เปิดไฟล์ Google Sheet → **ส่วนขยาย → Apps Script**
2. วางโค้ด `Code.gs` ใหม่ทับของเดิม (ลบเก่า → วางใหม่) → บันทึก
3. วางโค้ด `Index.html` ใหม่ทับของเดิม → บันทึก
4. มุมขวาบน กด **ทำให้ใช้งานได้ (Deploy)** → **จัดการการทำให้ใช้งานได้ (Manage deployments)**
5. กดไอคอน **✏️ ดินสอ (แก้ไข/Edit)**
6. ช่อง **เวอร์ชัน (Version)** เลือก **เวอร์ชันใหม่ (New version)**
7. กด **ทำให้ใช้งานได้ (Deploy)**
8. ✅ **ลิงก์เดิมใช้ได้เหมือนเดิม** ไม่ต้องส่งลิงก์ใหม่ให้ใคร

> ⚠️ **สำคัญ:** ถ้าเลือก "New deployment" (ไม่ใช่ Manage) ลิงก์จะ **เปลี่ยนใหม่** ต้องส่งลิงก์ใหม่ให้ทุกคน
> ดังนั้นตอนอัปเดตให้ใช้ **"Manage deployments"** เสมอ

---

## 🆘 ปัญหาที่พบบ่อย และวิธีแก้

| อาการ | สาเหตุ | วิธีแก้ |
|-------|--------|---------|
| ขึ้นว่า **"ไม่พบชีตข้อมูลนักเรียน RT, NT หรือ O-NET"** | ไฟล์ยังเป็น Excel ไม่ได้แปลงเป็น Google Sheets / ชีตไม่มีหัวตาราง "ชื่อ-สกุล" | กลับไปทำ **ขั้นตอนที่ 1** ให้แปลงเป็น Google ชีต และตรวจว่าตารางมีหัวข้อ "ชื่อ-สกุล / เลขที่นั่งสอบ / ลำดับ" |
| เปิดลิงก์แล้ว **เป็นหน้าว่าง / เปิดไม่ขึ้น** | สร้างไฟล์ HTML ผิดชื่อ | ตรวจว่าชื่อไฟล์คือ **`Index`** เป๊ะ ๆ (I ตัวใหญ่) |
| **แก้คะแนนในชีตแล้ว แต่หน้าเว็บไม่เปลี่ยน** | ระบบจำข้อมูลเดิมไว้ชั่วคราว (แคช) | ในหน้าเว็บ กดปุ่ม **"🔄 อัปเดตข้อมูลชีต"** ที่มุมบน |
| **อัปเดตโค้ดแล้ว หน้าเว็บยังเป็นแบบเก่า** | เบราว์เซอร์จำหน้าเก่าไว้ | กด `Ctrl + Shift + R` (ล้างแคช) หรือเปิดในหน้าต่าง **ไม่ระบุตัวตน (Incognito)** |
| ขึ้น **"Google hasn't verified this app"** | เป็นเรื่องปกติของแอปที่สร้างเอง | กด **Advanced → Go to ... (unsafe) → Allow** (ดูขั้นตอนที่ 6) |
| ครูคนอื่นเปิดลิงก์ **ไม่ได้ / ต้องขอสิทธิ์** | ตอน Deploy ตั้งค่า "ผู้ที่มีสิทธิ์เข้าถึง" เป็นเฉพาะตัวเอง | Deploy → Manage deployments → แก้ไข → เปลี่ยน "Who has access" เป็น **ทุกคน (Anyone)** |
| หน้าเว็บ **โหลดช้าครั้งแรก** | ระบบกำลังอ่านคะแนนทั้งหมดครั้งแรก | รอสักครู่ (มีวงกลมหมุน ⏳) ครั้งต่อไปจะเร็วขึ้นมาก |

---

## 📞 สรุปขั้นตอนแบบสั้น (Checklist)

- [ ] 1. อัปโหลดไฟล์คะแนน → แปลงเป็น **Google Sheets** (สีเขียว)
- [ ] 2. เปิดชีต → **ส่วนขยาย → Apps Script**
- [ ] 3. วางโค้ด **Code.gs** → บันทึก
- [ ] 4. สร้างไฟล์ HTML ชื่อ **`Index`** → วางโค้ด **Index.html** → บันทึก
- [ ] 5. **Deploy → New deployment → Web app** → ตั้งค่า → Deploy
- [ ] 6. **อนุญาตสิทธิ์** (Advanced → Go to → Allow)
- [ ] 7. **คัดลอกลิงก์** `.../exec` → เปิดใช้งาน 🎉

---

*หากทำตามครบทุกขั้นแล้วยังติดปัญหา ให้ถ่ายรูปหน้าจอที่ค้างอยู่ พร้อมข้อความ error แล้วส่งให้ผู้ดูแลระบบช่วยดูครับ*
